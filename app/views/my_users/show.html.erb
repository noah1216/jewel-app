<%= render "shared/header" %> 

<div class="contents-row">
  <div class="mypage-main">
    <div class="mypage-profile-box">
      <h2><%= @user.nickname %></h2>
      <%= image_tag("profile_icon.png", class:"profile-icon") %>
      <% if current_user == @user.id %>
      <div class="mypage-update">
        <%= link_to 'ユーザー編集',  edit_my_user_path(current_user.id), class: "default_link_white" %>
      </div>
      <% end %>
    </div>
  </div>

    <div class='items-search-box'>
    <div class='items-contents'>
      <h2>お気に入り一覧</h2>
    </div>
    <div class="mama">
    <ul class='item-lists'> 
      <% if @favorite_list.present? %>
      <% @favorite_list.each do |item| %>
          <li class='list'>
            <%= link_to item_path(item.id),class: "default_link_none" do %>
            <div class='item-img-content'>
              <%= image_tag item.image, class: "item-img" %>
              <% if item.order != nil  %> 
                <div class='sold-out'>
                  <span>Sold Out!!</span> 
                </div> 
              <% end %> 
            </div>
            <div class='item-info'>
              <h3 class='item-name'>
                <%= item.name %>
              </h3>
              <div class='item-price'>
                <span><%= item.price %>円<br><%= item.shopping_fee.name %></span>
              </div>
            </div>
            <% end %>
          </li>
      <% end %>
    <% end %>
      </ul>
  </div>
</div>